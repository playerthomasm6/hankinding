var passwordlength = (Math.floor(Math.random() *(selectlength)));
console.log(select);

var generateBtn = document.querySelector("#generate");

// Write password to the #password input
function writePassword() {
  var password = generatePassword();
  var passwordText = document.querySelector("#password");

  passwordText.value = password;

}

// Add event listener to generate button
generateBtn.addEventListener("click", writePassword);















var select = (Math.floor(Math.random() *4));
console.log(select);


var strselect = (chrpool[select]);
console.log(strselect);

//for loop to run character selector the specified # of times
//for (let i= 0; i < (selectlength + 1); i++){
  //var passwordlength = (Math.floor(Math.random() *(selectlength)));
  //console.log(select);

//}  



//var passwordlength = (Math.floor(Math.random() *(selectlength)));
//console.log(select);

//for loop to run character selector the specified # of times
//for (let i= 0; i < (selectlength + 1); i++){
  //var passwordlength = (Math.floor(Math.random() *(selectlength)));
  //console.log(select);

//}  



//var passwordlength = (Math.floor(Math.random() *(selectlength)));
//console.log(select);